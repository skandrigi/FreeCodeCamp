function telephoneCheck(str) {
  let testExp = /^(1\s?)?(\(\d{3}\)|\d{3})\s?\-?\d{3}\s?\-?\d{4}$/gi
  if (testExp.test(str) === true) {
    return true;
  }
  else {
    return false;
  }
}

telephoneCheck("555-555-5555");
